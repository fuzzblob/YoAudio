#pragma once

// NOTE:	This file is being generated by the CMake build process.
// 			Don't change this file by hand (except for testing during development)
//			Change YoaConfig.h.in instead please.

// version number set through CMake project build process
#define PROJECT_VERSION			@PROJECT_VERSION@
#define YOA_VERSION_MAJOR		@YOA_VERSION_MAJOR@
#define YOA_VERSION_MINOR		@YOA_VERSION_MINOR@
#define YOA_VERSION_PATCH		@YOA_VERSION_PATCH@

// toggle certain features of YoAudio
#cmakedefine SDL2_FOUND				@SDL2_FOUND@
#if SDL2_FOUND
#define FORCE_SDL @FORCE_SDL@
#else
#pragma error "can't find SDL2 dependency!"
#endif

#ifdef _DEBUG
#cmakedefine SPDLOG_FOUND			@SPDLOG_FOUND@
#if SPDLOG_FOUND
#define LOGGING_ENABLED			@LOGGING_ENABLED@
#else
#define LOGGING_ENABLED 		false
#endif
#else
#define LOGGING_ENABLED 		false
#endif

#define AUDIO_THREAD_UPDATES	@AUDIO_THREAD_UPDATES@
// calls to YoManager::Update() per second
#define TICK_RATE				@TICK_RATE@
// set to 0 for no limit on playing voices
#define MAX_VOICES 				@MAX_VOICES@

// audio device configuration
#cmakedefine TARGET_SAMPLERATE		@TARGET_SAMPLERATE@
#cmakedefine TARGET_CHANNELS			@TARGET_CHANNELS@
#cmakedefine TARGET_BUFFER			@TARGET_BUFFER@
#cmakedefine TARGET_BITDEPTH			@TARGET_BITDEPTH@