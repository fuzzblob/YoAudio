#pragma once

// NOTE:	This file is being generated by the CMake build process.
// 			Don't change this file by hand (except for testing during development)
//			Change EditorConfig.h.in instead please.

#include "../src/AudioFormat.h"

#include <cstdint>
#include <string>

constexpr const std::string PROJECT_NAME = "@PROJECT_NAME@";
// project version set through CMake project build process
constexpr const std::string PROJECT_VERSION = "@PROJECT_VERSION@";
constexpr uint16_t @PROJECT_NAME@_VERSION_MAJOR = @YOA_VERSION_MAJOR@u;
constexpr uint16_t @PROJECT_NAME@_VERSION_MINOR = @YOA_VERSION_MINOR@u;
constexpr uint16_t @PROJECT_NAME@_VERSION_PATCH = @YOA_VERSION_PATCH@u;

// toggle certain features of YoAudio
#define SDL2_FOUND @SDL2_FOUND@
#define LOGGING_ENABLED			@LOGGING_ENABLED@
#if LOGGING_ENABLED
#cmakedefine SPDLOG_FOUND			@SPDLOG_FOUND@
#endif

constexpr bool AUDIO_THREAD_UPDATES = @AUDIO_THREAD_UPDATES@;
// if no automatic audio thread updates,
constexpr uint16_t WAIT_TIME_MILLI_SECONDS = 10;
// calls to YoManager::Update() per second
constexpr uint16_t TICK_RATE = @TICK_RATE@u;
// set to 0 for no limit on playing voices
constexpr uint16_t MAX_VOICES = @MAX_VOICES@u;
// default minimum fade length
constexpr float MIN_FADE_LENGTH = 0.01f;

// audio device configuration
#cmakedefine TARGET_SAMPLERATE		@TARGET_SAMPLERATE@
#cmakedefine TARGET_CHANNELS			@TARGET_CHANNELS@
#cmakedefine TARGET_BUFFER			@TARGET_BUFFER@
#cmakedefine TARGET_BITDEPTH			@TARGET_BITDEPTH@
constexpr auto TargetRenderBitdepth = static_cast<SampleFormat>(@TARGET_BITDEPTH@);
